image: cirrusci/flutter:latest

definitions:
  caches:
    flutter: ~/.pub-cache

pipelines:
  default:
    - step:
        name: Build and Test Flutter App
        caches:
          - flutter
        script:
          - flutter pub get
          - flutter test
